{
  "name": "server",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "dev": "nodemon src/index.ts",
    "build": "npm ci && rm -rf build && npx tsc --project ./",
    "build:local": "rm -rf build && npx tsc --project ./",
    "start": "node build/index.js"
  },
  "author": "And",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.7",
    "ts-node": "^10.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-ws": "^4.0.0"
  }
}
