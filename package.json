{
  "name": "paint",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix server && npm run build --prefix client",
    "start": "cross-env NODE_ENV=production npm run start --prefix server",
    "build:local": "npm run build:local --prefix server && npm run build:local --prefix client"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/and-bulk/paint.git"
  },
  "author": "And",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/and-bulk/paint/issues"
  },
  "homepage": "https://github.com/and-bulk/paint#readme",
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "typescript": "^4.2.4"
  }
}
