{
  "name": "paint",
  "version": "1.0.0",
  "scripts": {
    "ci": "npm ci --prefix client && npm ci --prefix server",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "npm start --prefix server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/and-bulk/paint.git"
  },
  "author": "And",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/and-bulk/paint/issues"
  },
  "homepage": "https://github.com/and-bulk/paint#readme",
  "engines": {
    "node": "12.x"
  }
}
