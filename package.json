{
  "name": "paint",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix server && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "build:project": "npm run build --prefix server && npm run buildFolder --prefix client"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/and-bulk/paint.git"
  },
  "author": "And",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/and-bulk/paint/issues"
  },
  "homepage": "https://github.com/and-bulk/paint#readme",
  "engines": {
    "node": "12.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-ws": "^4.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.10",
    "@types/express": "^4.17.11",
    "@types/express-ws": "^3.0.0",
    "@types/node": "^15.0.1",
    "cross-env": "^7.0.3",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}
